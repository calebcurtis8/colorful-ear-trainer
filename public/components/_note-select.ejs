<span class="flex items-center justify-center col-span-2">
  <input class="hidden peer" id="note-select-<%= index %>" type="checkbox" name="custom-note-<%= index %>" value="<%= index %>" data-template="{<%= index %>}">
  <label class="relative flex items-center justify-center w-8 h-8 rounded-full cursor-pointer select-none peer-checked:bg-blue-500 peer-checked:font-semibold bg-theme-bg text-theme-text" for="note-select-<%= index %>">
    <span class="absolute" data-template="{<%= index %>}"><%= index %></span>
  </label>
</span>